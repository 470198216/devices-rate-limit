# 安卓笔记本电脑限速（不改路由器）

目标：整机约 **10 kbit/s**（≈ 1.25 KB/s）。优先用应用，没有 root 时终端里的 `tc` 跑不了。

单位别混：应用里若填 **KB/s**，10 kbit 应填约 **1.25**；填 **kbps / kbit** 才是 **10**。

上传、下载是否真的限住，按文末 **「怎么确认生效」** 分开测，不要只看网页卡不卡。

---

## 方案 A（推荐，免 root）：NetValve

本地 VPN 限速，流量不出本机、无远程服务器。GitHub 开源：https://github.com/IEAmir/NetValve

1. 用浏览器打开 https://github.com/IEAmir/NetValve/releases  
2. 下载 **`NetValve.apk`**（约 63MB，带真实转发）。不要用说明里的 loopback/`app-debug.apk`，那个不转发流量。  
3. 允许「未知来源」安装。  
4. 打开应用 → 允许 **VPN 连接**（系统会弹「是否允许创建 VPN」）。  
5. 关掉省电限制（华为/小米/OV 等笔记本 ROM 很容易把前台服务杀掉）。  
6. Apps 里勾选要限的应用（或能勾的尽量全勾）→ 上传/下载都设成 **10 kbps**。  
7. Dashboard 打开总开关。

测完关掉总开关，或卸载即可恢复。

**和其它「VPN」冲突：** Clash / v2rayNG / 公司 VPN / 系统级 TUN 同一时间只能有一个。先把那些关掉再开 NetValve。

备选（同样是本机 VPN，不是翻墙）：[Pausix](https://pausix.com/)（有慢速预设；若最低是 64 kbps，就比 10 kbit 宽松一些）。

---

## 方案 B：终端 + root

把 `limit-rate-android.sh` 拷到设备，例如 `/sdcard/limit-rate-android.sh`。

**必须用 Unix 换行。** 从 Windows 直接拷贝若仍报 `unexpected 'elif'` 或 `inaccessible or not found`，在电脑上不要用记事本另存；用已改好的这份（LF），或在 Termux 里先执行：

```sh
sed -i 's/\r$//' /sdcard/limit-rate-android.sh
```

Termux 或系统终端：

```sh
su -c 'sh /sdcard/limit-rate-android.sh apply 10'
su -c 'sh /sdcard/limit-rate-android.sh apply 10 down'
su -c 'sh /sdcard/limit-rate-android.sh apply 10 up'
su -c 'sh /sdcard/limit-rate-android.sh status'
su -c 'sh /sdcard/limit-rate-android.sh remove'
```

- `apply 10`：上传 + 下载都限 10kbit  
- `apply 10 down`：只限下载  
- `apply 10 up`：只限上传  

下载走网口**入口**，和上传不是同一条 `tc` 规则。脚本会先试 `ifb`（把入口转到虚拟口再 tbf）；安卓内核常常没有 ifb，则改用 **ingress police**（超速的包直接丢掉，TCP 会自己降到限速附近）。

若提示 `DOWNLOAD failed`，说明这台内核没有 ifb、也没有 police/filter，终端限不了下载，请改用 NetValve（方案 A）把下载也设成 10 kbps。

网口名脚本会自动从默认路由取（常见 `wlan0`，插网线可能是 `eth0`）。

---

## 怎么确认生效（上传 / 下载分开验）

**10 kbit/s ≈ 1250 字节/秒 ≈ 1.25 KB/s。**  
TCP/突发会让瞬时值偏高，**平均落在约 800–2500 字节/秒** 算生效。若仍是几十 KB/s 或 MB/s，说明这一方向没限住。

测之前：关掉 Clash / v2ray 等其它 VPN；方案 B 用 `status` 看网口名是不是正在上网的那个（常见 `wlan0`）。

建议顺序：**先对照（不限速）→ 只限下载 → 只限上传 → 双向 → 取消后再测一遍。**

### 0. 对照：先看无限速有多快

Termux（测速本身不必 root）：

```sh
# 下载对照（字节/秒）
curl -o /dev/null -s -w 'DOWN %{speed_download}\n' \
  'https://speed.cloudflare.com/__down?bytes=1000000'

# 上传对照（先造 200KB 再传）
dd if=/dev/zero of=/data/local/tmp/up.bin bs=1024 count=200 2>/dev/null
curl -o /dev/null -s -w 'UP %{speed_upload}\n' \
  -T /data/local/tmp/up.bin https://speed.cloudflare.com/__up
```

记下两个数字，后面限速后应明显掉到 KB/s 以下。

方案 B 若没有 `/data/local/tmp` 写权限，把 `up.bin` 改到 `~/up.bin`。没有 `curl`：`pkg install curl`。

### 1. 只验下载

```sh
su -c 'sh /sdcard/limit-rate-android.sh apply 10 down'
su -c 'sh /sdcard/limit-rate-android.sh status'
```

`status` 里应出现下面之一：

- `DOWNLOAD ok: ifb ...` 或 `ingress police`（apply 当时的打印）
- `---- wlan0 ingress (download) ----` 下面有 `ffff:` / `police` / `ifb0` 的 tbf

然后测下载（**不要**同时跑大上传）：

```sh
curl -o /dev/null -s -w 'DOWN %{speed_download}\n' \
  'https://speed.cloudflare.com/__down?bytes=200000'
```

| 结果 | 含义 |
|---|---|
| 约 800–2500 字节/秒 | 下载限速生效 |
| 和对照差不多快 | 下载没限住：看是否 `DOWNLOAD failed`，改用 NetValve |
| 完全卡住、速度 0 | police 丢包过狠或 DNS 被误伤，执行 `remove` 后换 NetValve |

浏览器打开任意网页：应极慢或长时间转圈，和「只限上传、网页仍能刷出来」不同。

### 2. 只验上传

先清掉下载规则，避免互相干扰：

```sh
su -c 'sh /sdcard/limit-rate-android.sh remove'
su -c 'sh /sdcard/limit-rate-android.sh apply 10 up'
su -c 'sh /sdcard/limit-rate-android.sh status'
```

`status` 里 `---- ... root (upload/egress) ----` 应有 `tbf rate 10Kbit`（或 `10kbit`）。

```sh
dd if=/dev/zero of=/data/local/tmp/up.bin bs=1024 count=200 2>/dev/null
curl -o /dev/null -s -w 'UP %{speed_upload}\n' \
  -T /data/local/tmp/up.bin https://speed.cloudflare.com/__up
```

| 结果 | 含义 |
|---|---|
| 约 800–2500 字节/秒 | 上传限速生效 |
| 仍然很快 | 限错网口，或测的是缓存/失败重试；看 `iface=` 是否为当前 Wi‑Fi/网线口 |
| 下载 `curl __down` 仍然很快 | 正常：当前只限了上传 |

此时浏览器往往还能打开页面（下载未限），但发邮件、传文件、同步会明显变慢。

### 3. 双向一起验

```sh
su -c 'sh /sdcard/limit-rate-android.sh apply 10'
```

分别再跑一遍上面的 DOWN / UP 两条 `curl`。两个方向都应慢。网页几乎打不开。

不要两条 `curl` 同时跑，10kbit 会被抢光，数字不好读。

### 4. 复原对照（防止「网本来就慢」误判）

```sh
su -c 'sh /sdcard/limit-rate-android.sh remove'
curl -o /dev/null -s -w 'DOWN %{speed_download}\n' \
  'https://speed.cloudflare.com/__down?bytes=1000000'
```

速度应回到第 0 步量级。回不来就检查是否还有 NetValve/其它 VPN 开着。

### 5. 方案 A（NetValve）怎么验

应用里上传、下载都填 **10 kbps**（不是 10 KB/s），打开 Dashboard。然后同样跑第 1、2 步的 `curl`（不必 `su`）。测完关掉总开关，再跑第 4 步确认恢复。

### 6. 常见误判

- **单位**：`speed_download` / `speed_upload` 是 **字节/秒**。10 kbit/s ≈ **1250**，不是 10、也不是 10240。
- **字节太少**：`bytes=200000` 在限速下要跑一两分钟，请等它跑完。对照用 `bytes=1000000` 才容易看出差异。
- **只看瞬时**：开头几秒可能突发偏快，看整次 `curl` 结束时打印的平均值。
- **限错方向**：只 `apply 10 up` 时，下载快是预期现象。
- **IPv6 绕过**：police 若只打上 ipv4，下载仍快；脚本会尽量加 ipv6。仍绕过就用 NetValve。

测完方案 A 关开关，方案 B 执行 `remove`。
